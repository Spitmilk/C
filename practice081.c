//题目：809*??=800*??+9*?? 其中??代表的两位数, 809*??为四位数，8*??的结果为两位数，9*??的结果为3位数。求??代表的两位数，及809*??后的结果。
#include <stdio.h>

int main()
{
    int i,j,k;
    for (i=10;i<20;i+=10)
    {
        for (j=0;j<10;j++)
        {
            if (809*i+809*j == 800*i+800*j+9*i+9*j && 9*i+9*j>=100 && 809*i+809*j<=9999 && 8*i+8*j<=99)
            {
                printf ("?? is:%d%d \n",i/10,j);
            }
        }
    }
    return 0;
}